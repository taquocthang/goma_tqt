import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    ratingWrap: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    ratingPoint: {
        fontSize: 15,
        color: '#CC0000',
        marginLeft: 10,
    },
})
export default styles;